APP_NAME=GAIS
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=Asia/Jakarta
# Samakan dengan alamat yang benar benar Anda ketik di peramban.
# Di Windows, localhost sering diarahkan ke ::1 lebih dulu sedangkan php artisan serve
# hanya mendengarkan di 127.0.0.1, dan permintaan ke localhost jadi menggantung.
APP_URL=http://127.0.0.1:8000

APP_LOCALE=id
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=id_ID

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=gais
DB_USERNAME=postgres
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=480
SESSION_ENCRYPT=false

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

MAIL_MAILER=log
MAIL_FROM_ADDRESS="ga@contoh.local"
MAIL_FROM_NAME="${APP_NAME}"

# Akun administrator pertama, dibuat oleh AdminUserSeeder.
GAIS_ADMIN_NAME=Administrator
GAIS_ADMIN_EMAIL=admin@gais.test
# Biarkan kosong supaya kata sandi acak dibuatkan dan dicetak sekali di layar saat seeding.
GAIS_ADMIN_PASSWORD=
